/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.uzum.uz",
      "uic.group",
      "images.ctfassets.net",
      "wac-cdn.atlassian.com",
      "plus.unsplash.com",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
